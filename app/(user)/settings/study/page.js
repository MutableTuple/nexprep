import StudySettings from "@/app/_components/Settings/StudySettings";

export const metadata = {
  title: "Study Settings",
  robots: { index: false, follow: false },
};

export default function Page() {
  return <StudySettings />;
}
